#include <stdio.h>

int main() {
    int a[]={4,7,3,9};
    int temp,i,j,min;
    for(i=0;i<3;i++){
        min=i;
   for(j=i+1;j<4;j++){
    if (a[j]< a[min]){
        min=j;}}
        temp=a[i];
        a[i]=a[min];
        a[min]=temp;
   
    }
    for(i=0;i<4;i++){
        printf("%d",a[i]);
    }
    return 0;
    
}
